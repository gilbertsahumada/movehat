# Dockerfile para testing de MoveHat
# Simula instalación limpia en diferentes versiones de Node.js
# 
# Build:
#   docker build -f Dockerfile.test --build-arg NODE_VERSION=20 -t movehat-test .
#
# Run:
#   docker run --rm movehat-test ./scripts/e2e-local.sh --quick

ARG NODE_VERSION=20
FROM node:${NODE_VERSION}-bookworm

# Install system dependencies
RUN apt-get update && apt-get install -y \
    curl \
    git \
    build-essential \
    ca-certificates \
    unzip \
    && rm -rf /var/lib/apt/lists/*

# Install the verified linux/amd64 Movement CLI artifact used by CI. Both the
# archive and extracted executable are checked: a failed download, a silent
# upstream re-upload, or an unusable binary fails the image build.
ARG MOVEMENT_CLI_ARCHIVE_SHA256=f2bdf3fa82e6b49c83c91be0967640282e1759e66b237440709a8779a9f9c1b2
ARG MOVEMENT_CLI_BINARY_SHA256=1101983bddae0e8f810d3a4b8e2b0f04d00f91ee5eb7f1cdefa3f93ad77ee1fa
RUN curl -fL -o /tmp/movement-cli.tar.gz \
      https://github.com/movementlabsxyz/homebrew-movement-cli/releases/download/bypass-homebrew/movement-cli-l1-linux-x86_64.tar.gz \
    && echo "${MOVEMENT_CLI_ARCHIVE_SHA256}  /tmp/movement-cli.tar.gz" | sha256sum -c - \
    && tar -xzf /tmp/movement-cli.tar.gz -C /usr/local/bin movement \
    && chmod 0755 /usr/local/bin/movement \
    && echo "${MOVEMENT_CLI_BINARY_SHA256}  /usr/local/bin/movement" | sha256sum -c - \
    && rm /tmp/movement-cli.tar.gz \
    && movement --version

# Install pnpm
RUN npm install -g pnpm@9

# Set working directory
WORKDIR /app

# Copy package files first (for better caching)
COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
COPY packages/movehat/package.json ./packages/movehat/

# Install dependencies
RUN pnpm install --frozen-lockfile

# Copy the rest of the project
COPY . .

# Build the project
RUN pnpm build:movehat

# Set environment for testing
ENV NODE_ENV=test
ENV CI=true

# Default command: run E2E tests
CMD ["./scripts/e2e-local.sh", "--quick"]
