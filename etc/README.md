# Public API reports

The Markdown files in this directory are generated by API Extractor and are
committed intentionally. Run `pnpm api:update` only when a public API change is
intentional and include the report diff in review. `pnpm api:check` is read-only:
it fails when the built declarations differ from these reviewed snapshots.
