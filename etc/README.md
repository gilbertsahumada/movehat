# Public API reports

The Markdown files in this directory are generated by API Extractor and are
committed intentionally. Run `pnpm api:check` after changing a public export;
CI fails when the generated surface differs from these reviewed snapshots.
